// Syntax, logical and runtime errors

// Here are some of the most common errors in JavaScript: 
// ReferenceError 
// SyntaxError 
// TypeError 
// RangeError
// There are some other errors in JavaScript. These other errors include: 
// AggregateError 
// Error 
// InternalError 
// URIError

// 1. ReferenceError
// A ReferenceError gets thrown when, for example, one tries to use variables that haven't been declared anywhere.
// 2. SyntaxError
// Any kind of invalid JavaScript code will cause a SyntaxError.
// 3. TypeError
// A TypeError is thrown when, for example, trying to run a method on a non-supported data type.
// A simple example is attempting to run the pop() method on a string:
// 4. A RangeError is thrown when we're giving a value to a function, but that value is out of the allowed range of acceptable input values.

// 3 types of empty values: the null datatype, the undefined datatype, and empty strings.